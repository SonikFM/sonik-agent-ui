# Storybook exception

`apps/dev-workbench` does not currently install or configure Storybook. The
shared `devWorkbenchStartingFixture` and `devWorkbenchReadyFixture` cover the
unavailable and ready visual states until the repository adds an app-wide
Storybook surface. Do not add a component-local Storybook dependency.
